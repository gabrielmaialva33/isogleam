export function do_get(url, headers) {
  return { _tag: "Error", _0: "Synchronous HTTP not supported in JavaScript target" };
}

export function do_post_json(url, body, headers) {
  return { _tag: "Error", _0: "Synchronous HTTP not supported in JavaScript target" };
}
